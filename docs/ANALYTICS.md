# Analytics (deferred)

Analytics is not configured. A future implementation must minimize personal data, disclose collection, keep provider keys server-side where required, capture only product questions (for example conversion or completion), and offer deletion/export paths when retained user data exists.
