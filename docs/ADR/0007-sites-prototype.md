# ADR 0007: Sites for validation, not production backend

## Decision

Use Sites for prototype and UX validation, not the final paid backend.

## Consequence

Approved designs migrate into `apps/web` and `packages/ui`; security and product rules remain in the production repository.
